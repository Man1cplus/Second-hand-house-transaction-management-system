<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.secondhand.secondhandhousebackend.mapper.PropertiesMapper">

    <resultMap id="BaseResultMap" type="org.secondhand.secondhandhousebackend.entity.Properties">
            <id property="propertyid" column="PropertyID" />
            <result property="title" column="Title" />
            <result property="description" column="Description" />
            <result property="price" column="Price" />
            <result property="area" column="Area" />
            <result property="layout" column="Layout" />
            <result property="orientation" column="Orientation" />
            <result property="floor" column="Floor" />
            <result property="decoration" column="Decoration" />
            <result property="buildingType" column="BuildingType" />
            <result property="buildYear" column="BuildYear" />
            <result property="elevatorRatio" column="ElevatorRatio" />
            <result property="address" column="Address" />
            <result property="longitude" column="Longitude" />
            <result property="latitude" column="Latitude" />
            <result property="province" column="Province" />
            <result property="city" column="City" />
            <result property="district" column="District" />
            <result property="street" column="Street" />
            <result property="publishdate" column="PublishDate" />
            <result property="status" column="Status" />
            <result property="sellerid" column="SellerID" />
    </resultMap>

    <!-- 自定义查询：使用 resultMap 确保字段正确映射 -->
    <select id="selectAllWithResultMap" resultMap="BaseResultMap">
        SELECT 
            PropertyID, Title, Description, Price, Area, Layout,
            Orientation, Floor, Decoration, BuildingType, BuildYear, ElevatorRatio,
            Address, Longitude, Latitude, Province, City, District, Street,
            PublishDate, Status, SellerID, Photos
        FROM properties
    </select>

    <!-- 根据条件查询房源 -->
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT 
            PropertyID, Title, Description, Price, Area, Layout,
            Orientation, Floor, Decoration, BuildingType, BuildYear, ElevatorRatio,
            Address, Longitude, Latitude, Province, City, District, Street,
            PublishDate, Status, SellerID, Photos
        FROM properties
        WHERE Status = #{status}
    </select>

    <!-- 根据卖家ID查询房源 -->
    <select id="selectBySellerId" resultMap="BaseResultMap">
        SELECT 
            PropertyID, Title, Description, Price, Area, Layout,
            Orientation, Floor, Decoration, BuildingType, BuildYear, ElevatorRatio,
            Address, Longitude, Latitude, Province, City, District, Street,
            PublishDate, Status, SellerID, Photos
        FROM properties
        WHERE SellerID = #{sellerid}
    </select>

    <!-- 根据ID查询单个房源（使用 resultMap 确保字段正确映射） -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT 
            PropertyID, Title, Description, Price, Area, Layout,
            Orientation, Floor, Decoration, BuildingType, BuildYear, ElevatorRatio,
            Address, Longitude, Latitude, Province, City, District, Street,
            PublishDate, Status, SellerID, Photos
        FROM properties
        WHERE PropertyID = #{propertyid}
    </select>

</mapper>
